---
functions:
  library-load:
  - code: |-
      mosquitto_ctrl libgtfo cmd
    comment: |-
      To pass validation prior to loading, the name of the library must follow the pattern mosquitto_ctrl_<libname>.so,
      where only libname is included as an argument in the mosquitto_ctrl command.
    contexts:
      unprivileged:
...
